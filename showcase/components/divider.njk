---
title: Divider
layout: layout.njk
---

<section class="demo-section">
  <h1>Divider</h1>

  <!-- Horizontal Divider -->
  <div class="demo-row">
    <div class="demo-label">Horizontal Divider (hr.divider)</div>
    {% demo %}
    <p>Content above</p>
    <hr class="divider" />
    <p>Content below</p>
    {% enddemo %}
  </div>

  <!-- Text Divider -->
  <div class="demo-row">
    <div class="demo-label">Text Divider (div.divider)</div>
    {% demo %}
    <div class="divider">Section Title</div>
    <div class="divider">OR</div>
    {% enddemo %}
  </div>

  <!-- Vertical Divider -->
  <div class="demo-row">
    <div class="demo-label">Vertical Divider (span.divider.vertical)</div>
    {% demo %}
    <div style="display: flex; align-items: center; overflow-x: auto">
      <span>Left Content</span>
      <span class="divider vertical"></span>
      <span>Middle Content</span>
      <span class="divider vertical"></span>
      <span>Right Content</span>
    </div>
    {% enddemo %}
  </div>
</section>
